# {{ ansible_managed }}

[Unit]
After=syslog.target

[Service]
ExecStart=/home/vagrant/venv/bin/uwsgi --ini {{ api_dev_uwsgi_path }}
Restart=always
KillSignal=SIGQUIT
Type=notify
StandardError=syslog
NotifyAccess=all

[Install]
WantedBy=multi-user.target
