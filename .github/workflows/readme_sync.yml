name: Sync README Badges

on:
  repository_dispatch:
    types: [sync-readme]
  workflow_dispatch:
  push:
    paths:
      - 'badges.yml'

jobs:
  update-readme:
    uses: renalreg/.github/.github/workflows/reusable_badges.yml@main
    secrets: inherit


